unifi_role:
  directories:
    - path: "{{ unifi_dir }}"
      volume: unifi
    - path: "{{ unifi_dir }}/data/backup"
      volume: unifi-backup
  docker:
    service:
      name: unifi
      image: linuxserver/unifi-controller
      volumes:
        - unifi:/config
      ports:
        - 3478:3478/udp
        - 10001:10001/udp
        - 8080:8080
        - 8081:8081
        - 8443:8443
        - 8843:8843
        - 8880:8880
        - 6789:6789
